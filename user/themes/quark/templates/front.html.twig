{% extends 'partials/base.html.twig' %}

  {% block content %}
    <div id="front-banner">
        <h1 style="color:white;">{{site.title}}</h1>
        <h2 style="color:white;">{{site.author.name}}</h2>
    </div>
      {{ page.content|raw }}
  {% endblock %}
